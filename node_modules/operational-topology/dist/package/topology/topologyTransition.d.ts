export type TopologyTransitionPhase = "idle" | "exiting" | "handoff" | "entering" | "complete" | "cancelled";
export interface TopologyTransitionFrame {
    phase: TopologyTransitionPhase;
    progress: number;
    outgoingOpacity: number;
    incomingOpacity: number;
    hierarchyProgress: number;
    gridOpacity: number;
}
export interface TopologyTransitionFrameOptions {
    elapsed: number;
    duration?: number;
    reducedMotion?: boolean;
}
export interface TopologyTransitionLayer {
    element: HTMLElement | SVGElement;
}
export interface TopologyTransitionControllerOptions {
    duration?: number;
    reducedMotion?: boolean | (() => boolean);
    onFrame?: (frame: TopologyTransitionFrame) => void;
    onPhase?: (phase: TopologyTransitionPhase) => void;
}
export interface TopologyTransitionController {
    run(input: {
        outgoing: TopologyTransitionLayer;
        incoming: TopologyTransitionLayer;
    }): Promise<void>;
    cancel(): void;
}
export declare function getTopologyTransitionFrame({ elapsed, duration, reducedMotion, }: TopologyTransitionFrameOptions): TopologyTransitionFrame;
export declare function getHierarchyRevealOpacity(depth: number, progress: number, depthCount?: number): number;
export declare function createTopologyTransitionController({ duration, reducedMotion, onFrame, onPhase, }?: TopologyTransitionControllerOptions): TopologyTransitionController;
//# sourceMappingURL=topologyTransition.d.ts.map