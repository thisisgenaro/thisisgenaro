import { type HexCell } from "./hex.ts";
export interface ViewportGridOptions {
    width: number;
    height: number;
    radius: number;
    cameraX?: number;
    cameraY?: number;
    padding?: number;
}
export interface VisibleGridCell extends HexCell {
    edgeFactor: number;
}
export declare function generateViewportCells({ width, height, radius, cameraX, cameraY, padding, }: ViewportGridOptions): VisibleGridCell[];
export declare function createViewportFitHexGrid(options: ViewportGridOptions): HexCell[];
//# sourceMappingURL=viewportGrid.d.ts.map