import { type ConnectorRoutingMode } from "./connectorRouting.ts";
import { type AxialCoord, type HexCell } from "./hex.ts";
export interface SignalRoutingOptions {
    mode: ConnectorRoutingMode;
    cells: Pick<HexCell, "id" | "q" | "r">[];
    occupants: AxialCoord[];
    from: AxialCoord;
    to: AxialCoord;
    radius: number;
}
export declare function createSignalRoute({ mode, cells, occupants, from, to, radius, }: SignalRoutingOptions): AxialCoord[];
//# sourceMappingURL=SignalRouting.d.ts.map