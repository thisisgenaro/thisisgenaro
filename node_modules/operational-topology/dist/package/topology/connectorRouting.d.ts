import { type AxialCoord, type HexCell, type PixelCoord } from "./hex.ts";
export type ConnectorRoutingMode = "straight" | "axial" | "obstacle-aware";
export type ConnectorRoutingStatus = "routed" | "fallback";
export interface ConnectorRoutingOptions {
    mode: ConnectorRoutingMode;
    cells: Pick<HexCell, "id" | "q" | "r">[];
    from: AxialCoord;
    to: AxialCoord;
    radius: number;
    blocked?: AxialCoord[];
    offset?: PixelCoord;
    bendPenalty?: number;
    proximityPenalty?: number;
    maxVisited?: number;
}
export interface RoutedConnector {
    mode: ConnectorRoutingMode;
    status: ConnectorRoutingStatus;
    cells: AxialCoord[];
    waypoints: AxialCoord[];
    points: PixelCoord[];
    d: string;
    bends: number;
    visited: number;
}
export declare function compressConnectorRoute(coordinates: AxialCoord[]): import("./hex.ts").AxialCoordinate[];
export declare function routeConnector(options: ConnectorRoutingOptions): RoutedConnector;
//# sourceMappingURL=connectorRouting.d.ts.map