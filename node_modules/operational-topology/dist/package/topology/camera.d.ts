import { type AxialCoord } from "./hex.ts";
export interface CameraState {
    center: AxialCoord;
    zoom: number;
    viewportWidth: number;
    viewportHeight: number;
}
export declare function createCameraState(state?: Partial<CameraState>): CameraState;
export declare function frameCameraToPoint(point: AxialCoord, radius: number, viewportWidth: number, viewportHeight: number): CameraState;
export declare function frameCameraToEntities(params: {
    entities: AxialCoord[];
    viewportWidth: number;
    viewportHeight: number;
    radius: number;
    padding?: number;
}): CameraState;
export declare function visibleCells(params: {
    cells: {
        id: string;
        center: {
            x: number;
            y: number;
        };
    }[];
    camera: CameraState;
    radius: number;
    padding?: number;
}): {
    id: string;
    center: {
        x: number;
        y: number;
    };
}[];
//# sourceMappingURL=camera.d.ts.map