import type { ConnectorRoutingMode } from "./connectorRouting.ts";
import type { TopologyGridCellState, TopologyRenderFrame } from "./renderPipeline.ts";
export interface TopologyGridMarkupOptions {
    frame: TopologyRenderFrame;
    radius: number;
    activeCellIds?: string[];
    cellStates?: Record<string, TopologyGridCellState>;
    showNodes?: boolean;
    className?: string;
    connectorRouting?: ConnectorRoutingMode;
}
export declare function renderTopologyGridMarkup({ frame, radius, activeCellIds, cellStates, showNodes, className, connectorRouting, }: TopologyGridMarkupOptions): string;
//# sourceMappingURL=TopologyGridRenderer.d.ts.map